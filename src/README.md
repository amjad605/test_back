1. `utils/validationErrors.ts`
2. `utils/S3Uploader.ts`, it already contains the methods for uploading and deleting
3. `src/auth`, it's already implemented fully with roles, will need few refinements
4. `src/blogs` contains the recommended folder structure
